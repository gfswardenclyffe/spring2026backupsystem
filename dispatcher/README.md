# Concurrent Task Dispatcher in Rust

## Overview
This project implements a concurrent task dispatcher in Rust. The system simulates incoming tasks, places them in a queue, and assigns them to a fixed worker pool using a scheduling policy.

The goal is to demonstrate concurrency concepts such as threads, channels, synchronization, and scheduling.

---

## How to Run

```bash
cargo run